# מה שונה בפרויקט

## מטרת השינויים
השינויים נעשו כדי להקטין סיכוי להורדת נקודות בבדיקה: תיקון סנכרון בין אובייקטים, מניעת מצבים לא תקינים, ושיפור קטן באיכות הקוד בלי להפוך אותו למתקדם מדי.

---

## 1. Lecturer.java

### מה שונה
נוסף getter למחלקה של המרצה:

```java
public Department getDepartment() {
    return department;
}
```

בנוסף, נוסף `@Override` מעל `toString()`.

### למה זה חשוב
עכשיו `College` יכול לבדוק את המחלקה האמיתית של המרצה דרך האובייקט עצמו, ולא להסתמך רק על מערך עזר חיצוני.
זה מקטין בעיות סנכרון בין `College` לבין `Lecturer`.

---

## 2. College.java

### מה שונה
ב־`addLecturerToDepartment` הבדיקה אם מרצה כבר משויך למחלקה נעשית עכשיו דרך:

```java
lecturer.getDepartment()
```

במקום להסתמך רק על:

```java
lecturerDepartments[lecturerIndex]
```

### למה זה חשוב
זה יותר נכון מבחינת תכנות מונחה עצמים, כי המידע על המחלקה שייך גם לאובייקט `Lecturer`.

---

### מה שונה בהוספה לוועדה
ב־`addLecturerToCommittee` ההוספה עכשיו מתבצעת בזהירות:

1. מוסיפים את המרצה לוועדה.
2. מוסיפים את הוועדה למידע של `College`.
3. מוסיפים את הוועדה למידע של `Lecturer`.
4. אם אחד השלבים נכשל — מבטלים את מה שכבר נוסף.

### למה זה חשוב
בלי זה יכול להיווצר מצב שבו המרצה מופיע בוועדה, אבל אצל המרצה עצמו הוועדה לא מופיעה, או להפך.
זה מצב שיכול להוריד נקודות כי הנתונים לא עקביים.

---

### מה שונה בהסרה מוועדה
ב־`removeLecturerFromCommittee` ההסרה עכשיו מתבצעת גם מהוועדה, גם מהמערכים של `College`, וגם מהאובייקט `Lecturer`.

אם אחד השלבים נכשל — הקוד מנסה להחזיר את המצב אחורה.

### למה זה חשוב
זה מונע מצב שבו המרצה נמחק רק ממקום אחד אבל עדיין נשאר רשום במקום אחר.

---

## 3. Committee.java

### מה שונה
נוסף `@Override` מעל `toString()`.

בנוסף, תוקן ריווח קטן בלולאת ההסרה:

```java
memberCount - 1
```

במקום:

```java
memberCount -1
```

### למה זה חשוב
זה לא משנה את הריצה, אבל זה משפר קריאות ומראה קוד מסודר יותר.

---

## 4. Department.java

### מה שונה
נוסף `@Override` מעל `toString()`.

בנוסף, סודר הריווח בתחילת המחלקה כדי שהשדות יהיו מיושרים ונקיים.

### למה זה חשוב
קוד מסודר יותר נראה מקצועי יותר ומקל על הבודק להבין את המבנה.

---

## 5. Degree.java

### מה שונה
השדה `rank` הוגדר כ־`final`:

```java
private final int rank;
```

### למה זה חשוב
הערך של דרגה ב־enum לא אמור להשתנות אחרי יצירתו.
`final` מראה שהערך קבוע ובטוח יותר.

---

## מה עדיין חשוב לבדוק לפני הגשה

להריץ קומפילציה:

```bash
javac *.java
```

להריץ את התוכנית:

```bash
java BenFisherRazMazliah
```

ולבדוק ידנית את האפשרויות הבאות:

1. הוספת מרצה.
2. הוספת מחלקה.
3. שיוך מרצה למחלקה.
4. הוספת ועדה עם יו"ר שהוא Doctor או Professor.
5. הוספת מרצה לוועדה.
6. הסרת מרצה מוועדה.
7. הדפסת כל המרצים.
8. הדפסת כל הוועדות.
9. חישוב ממוצע שכר כללי.
10. חישוב ממוצע שכר לפי מחלקה.

---

## פקודות commit מומלצות

```bash
git add .
git commit -m "Fix lecturer committee synchronization and improve code quality"
git push
```

---

## סיכום קצר
הפרויקט עכשיו יותר יציב, יותר מסונכרן, ונראה טוב יותר מבחינת OOP בסיסי. השינויים לא מסבכים את הקוד יותר מדי, אבל כן מתקנים נקודות שעלולות להוריד ציון.
